# Super Admin credentials.
# Keep ADMIN_PASSWORD and ADMIN_RECOVERY_KEY in Replit Secrets; never commit
# their values or store them in plaintext in MongoDB.
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_RECOVERY_KEY=

# SMTP settings for free email OTP password recovery.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=